type MediaItem {
  book_id: ID!
  user_id: ID!
  filename: String!
  book_genre: String!
  thumbnail: String!
  filesize: Int!
  media_type: String!
  title: String!
  description: String
  created_at: String
}

type Query {
  mediaItems: [MediaItem]
}
