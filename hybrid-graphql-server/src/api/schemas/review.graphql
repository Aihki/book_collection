type Review {
  review_id: ID!
  book: MediaItem!
  user: User!
  review_text: String!
  created_at: String!
}


type Query {
  review(book_id: ID!): [Review!]!

}

type Mutation {
  addReview(book_id: ID!, review_text:String!): Message
}

