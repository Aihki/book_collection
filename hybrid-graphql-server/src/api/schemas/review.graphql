type Review {
  review_id: ID!
  book: MediaItem!
  user: User!
  review_text: String!
  created_at: String!
}

type Query {
  reviews(book_id: ID!): [Review]
}

type Mutation {
  addReview(book_id: ID!, review:String!):Message
}

